# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

#  __   __   __    __  
# |  \ |  | |  |  |  | 
# |   \|  | |  |__|  |  Nick Hartjes
# |    `  | |   __   |  https://nickhartjes.nl
# |  |\   | |  |  |  |  https://github.com/nickhartjes/
# |__| \__| |__|  |__| 
#    

ZSH_THEME="powerlevel10k"

# Which plugins would you like to load?
# Standard plugins can be found in $ZSH/plugins/
# Custom plugins may be added to $ZSH_CUSTOM/plugins/
# Example format: plugins=(rails git textmate ruby lighthouse)
# Add wisely, as too many plugins slow down shell startup.
plugins=(
    aws
    colorize
    dnf
    docker
    docker-compose
    git
    git-flow
    helm
    kubectl
    npm
    ssh-agent
    zsh-syntax-highlighting
    zsh-autosuggestions
)

alias w='feh --randomize --bg-fill ~/.wallpapers/*'
alias ww='feh --randomize --bg-fill ~/.wallpapers/photos/*'
alias config='/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'

alias sshMaster='ssh -i ~/.ssh/linux-foundation-course-key.pem ubuntu@ec2-18-184-182-234.eu-central-1.compute.amazonaws.com'
alias sshNode='ssh -i ~/.ssh/linux-foundation-course-key.pem ubuntu@ec2-18-157-175-50.eu-central-1.compute.amazonaws.com'

source /usr/share/zsh-theme-powerlevel10k/powerlevel10k.zsh-theme

alias editZsh="vim ~/.zshrc-personal"
alias editAwesome="vim ~/.config/awesome/rc.lua"

# Dotfiles
alias .install="sh ~/.dotfiles/install/install.sh"
alias .installSoftware="sh ~/.dotfiles/install/arch/install.sh"
alias .sync="sh ~/.dotfiles/install/sync.sh"

# Desktop settings
alias dtScreens="xrandr --output DisplayPort-0 --primary --mode 2560x1440 --pos 0x604 --rotate normal --output DisplayPort-1 --mode 2560x1440 --pos 2560x0 --rotate left --output DisplayPort-2 --off --output HDMI-A-0 --off"
alias dtInit="spotify"

export PATH=~/.cargo/bin:$PATH
export CHROME_BIN=/usr/bin/chromium

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
